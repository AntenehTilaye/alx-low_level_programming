#include<stdio.h>

/**
 * main- the starting point
 * Return: it returns 0 for os
 * Description: the program prints a string to STD output
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
